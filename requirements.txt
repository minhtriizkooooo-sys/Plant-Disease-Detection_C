# Đảm bảo pip, setuptools, wheel mới nhất để cài prebuilt wheel
pip>=25.3
setuptools>=68.0.0
wheel>=0.41.2

# Pillow chọn phiên bản đã có wheel cho Python 3.11
pillow==10.3.0

# Các package khác của bạn (ví dụ)
flask==2.3.2
requests==2.31.0
numpy==1.27.0
pandas==2.1.1
